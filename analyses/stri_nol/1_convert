#Convert original videos from asf to mkv format 
#those are container formats
#use constant frame rate codec so that videos can by synced frame by frame
#original files are too large to keep on GitHub so links are present to 

#Need to use ffmpeg in base conda on laptop



cd ../../video_data/stri_nol/checkerboard/

##checkerboard videos
#ffmpeg -i 102338-1.ASF -f matroska -vcodec libx264 -an -framerate 30 -crf 0 102338-1.mkv
#ffmpeg -i 102338-2.ASF -f matroska -vcodec libx264 -an -framerate 30 -crf 0 102338-2.mkv

#Using handbrake to convert, this codec does not seem to keep constant frame rate?
