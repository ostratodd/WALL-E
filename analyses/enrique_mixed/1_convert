#Convert original videos from asf to mkv format 
#those are container formats
#use constant frame rate codec so that videos can by synced frame by frame
#original files are too large to keep on GitHub so links are present to 

#Need to use ffmpeg in base conda on laptop



cd ../../video_data/enrique_mixed/checkerboards

#checkerboard videos test 1
ffmpeg -i 161134-1.ASF -f matroska -vcodec libx264 -an -framerate 30 -crf 0 161134-1.mkv
ffmpeg -i 161135-2.ASF -f matroska -vcodec libx264 -an -framerate 30 -crf 0 161135-2.mkv

#checkerboard video test 2
ffmpeg -i 161949-1.ASF -f matroska -vcodec libx264 -an -framerate 30 -crf 0 161949-1.mkv
ffmpeg -i 161949-2.ASF -f matroska -vcodec libx264 -an -framerate 30 -crf 0 161949-2.mkv


#Dock test 2 using stand alone checkerboard 150716-1 and 150717-2
